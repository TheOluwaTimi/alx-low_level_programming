#include "main.h"
#include <stdio.h>

void print_rev(char *s)
{
    int 1, i;
    char ch;

    for (1 = 0; s(1) ! = '\0'; 1++)
			        
    for (i = 0; i < 1 / 2; i++)
    {
         ch = s(1);
         s(1) = s(1 - 1 - i);
         s(1 -1 -i) = ch;
    }
}
